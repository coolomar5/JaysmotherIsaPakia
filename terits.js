"use strict";
let canvas = document.getElementById("tetris");
const context = document.getElementById("2d");

context.scale = (20, 20)

function arenaSweep(){
    let rowCount = 1;
    outer: for(let y  = arena.Length; y>0; --y){s
        for(let x = 0; x<arena[y].Length; ++x){
            if(arena[y][x]==0){
                continue outer;
        }
        
        }

        
    }
}

